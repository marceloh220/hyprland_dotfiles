Este arquivo foi mantido apenas por compatibilidade.

A documentação principal do projeto está em README.md.

Comece por:

- `./install.sh --help`
- `./install.sh --list-packages-groups`

Instalador Python (legado):

- `python install.py --help`

